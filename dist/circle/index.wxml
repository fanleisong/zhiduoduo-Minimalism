<view class="van-circle">
    <canvas canvasId="van-circle" class="van-circle__canvas" style="{{style}}"></canvas>
    <view class="van-circle__text" wx:if="{{!text}}">
        <slot></slot>
    </view>
    <cover-view class="van-circle__text" wx:else>{{text}}</cover-view>
</view>
